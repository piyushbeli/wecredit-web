/** Child sitemap paths referenced by the parent sitemap index at `/sitemap.xml`. */
export const SITEMAP_CHILD_PATHS = {
  pages: '/sitemap-page.xml',
  posts: '/sitemap-posts.xml',
  loans: '/sitemap-loans.xml',
} as const;
